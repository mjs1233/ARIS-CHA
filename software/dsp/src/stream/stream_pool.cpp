//
// Created by takakura on 9/2/26.
//
#include "stream/stream_pool.hpp"
namespace aris::dsp {

    void stream_pool_manager::init() {

    }

    bool stream_pool_manager::alloc_pools() {

    }

    bool stream_pool_manager::add_pool_impl(
        details::type_id_t type_id,
        std::size_t type_size,
        std::size_t N,
        std::size_t count
        ) {

    }

    void* stream_pool_manager::acquire_impl(
        details::type_id_t type_id,
        std::size_t type_size,
        std::size_t N
        ) {

    }
}